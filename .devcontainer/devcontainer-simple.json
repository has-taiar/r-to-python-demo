{
  "name": "R Development Environment (Simple)",
  "image": "rocker/tidyverse:4.3.2",
  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r"
      ],
      "settings": {
        "r.rterm.linux": "/usr/local/bin/R",
        "r.rpath.linux": "/usr/local/bin/R"
      }
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libssl-dev libcurl4-openssl-dev libxml2-dev unixodbc unixodbc-dev && R -e \"install.packages(c('DBI','odbc','plotly','htmlwidgets'), repos='https://cran.rstudio.com/')\"",
  "remoteUser": "rstudio",
  "forwardPorts": [8787]
}
