# Database Environment Variables
# Copy this file to .env and update with your actual credentials
# Note: .env files should NEVER be committed to version control

DB_USERNAME=sqladminuser
DB_PASSWORD=PASSWORD

# Alternative connection strings if needed
# DB_CONNECTION_STRING=Driver={ODBC Driver 17 for SQL Server};Server=financial-mcp-demo-app-sql.database.windows.net;Database=FinancialDemoDb;UID=sqladminuser;PWD=Admin123!;
